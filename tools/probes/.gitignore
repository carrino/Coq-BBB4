ProbeLookup.v
# validation-sized samples + the per-rung catch-diffs cut from them
# (24k lines): regenerate with gen_tier_cost.py / gen_rwdiff.sh, see README
ProbeTierBig.v
ProbeRwDiff_*.v
*.vo
*.vos
*.vok
*.glob
*.aux
