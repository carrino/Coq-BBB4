Build the two knobs in `carrino/Coq-BBB4`, on branch `claude/ladder-arms-<yourid>` cut from main (PR #88 and #89 merged: the Stage-B kernel, and its closure — three machines boarded, core undecided 62 → 59 — §4h, §4i, §4k).

Read first, in this order: `docs/LADDER_PLAN.md` **§4k in full** — it is the reason this session exists and its second half is the specification; then §4i's "Where the other rows stop" and "The gate"; then `tools/ladder/core61_armshapes.txt`, which is the measurement you are implementing — **do not re-derive it, and do not re-run the sweep that produced it**; then `theories/Checkers/LadderCheck.v`, where `board_neverqh`'s hypothesis list IS the interface and `cls_side`, `run_side`, `fam_cells_class`, `fam_cells_run` are the four things you change; then `tools/ladder/emit_ladder.py`'s `closure_data` and `emit_closure`, which are 200 lines and are what you re-emit from.

**STATE.** 59 core undecided (`tools/closeout/core_rows.txt`; `make closeout-status` says 59). Of the 21 binary/step-1/one-phase rows in the live core, **3 are boarded and 18 fail on ARM DERIVATION** — not the glue, not the code, not the phase, not the liveness. §4i put a stride on the interior arm and called the rest "four rows"; that was half the story. With a stride **and** a materialisation offset on **both** class arms, 15 of 21 have both arms: 6 never-quasihalting (2 already boarded, **+4 new**) and 9 quasihalting. The stride exists on the interior arm only; the offset exists on neither; the fill arm has neither. `core61_armshapes.txt` gives the exact stride and offset per row per arm with the derived step counts. The wave route is working the same population concurrently — a candidate that sits unboarded decays.

**THE TASK — two pieces, in this order, and the second is worth exactly zero rows before the first.**

* **The two knobs, on both arms.** The interior class is `n = off + stride*j` and `cls_side F t off stride w` already denotes it for any `off` — the gap is that `board_lap` picks `off = n mod st`, so an arm needing `off ≥ st` leaves the small `n` uncovered. Give the board an arm index that is `n` itself below a threshold `N0` (a flat arm, `stride = 0`, everything concrete in `s_pre`) and `N0 + ((n − N0) mod st)` at or above it, with count `(n − N0) / st`; one scheme, stated once. Then give the **fill** arm the same two knobs — it has neither today, and that is why 6 of the 11 quasihalters and 4 of the 10 never-QH rows have no fill chain. `Nat.div_mod_eq` plus `lia` is the whole arithmetic. `board_neverqh` otherwise does not change.

* **The quasihalt closer.** `LapGlueQuiet.glue_qh_quiet` already ends in `NonHalt /\ QHBound (S s0) /\ QuasiHaltsSt` — exactly the triple `CloseoutKit.covers_iqh_at` consumes — and its `Hvis` is the same too-strong premise §4i weakened for `LapGlue`. Port it the same way: `nat`-indexed, "for every `N` there is an anchor past `N` from which `q` is reachable", for every `q ≠ qa`. The one genuinely new obligation is `AvoidRun tm qa n (Cf p)` in `Hlap`: the lap must never enter the quiet state. `Checkers/LapAvoid.v`'s `srun_avoid_sound` is that piece and it is `vm_compute`-able — but it is stated on `srun` over `list lstep` and the arms carry `list rstep`, so you need `base_chain : list rstep -> option (list lstep)` and the lemma that `rrun` on an all-`RB` chain is `srun`. Every arm is all-`RB` today. `qa = A` on all nine.

**DO ONE ROW END-TO-END FIRST.** `1RB1LA_0LA0RC_0LD0RB_1LD1RC` — binary, step 1, one phase, never-QH, digit width 1, interior stride 2 at offsets 2 and 1, fill stride 2. Its cert is already in `tools/ladder/core61_sweep.jsonl`; do not re-certify unless you change the emitter's family fields. Drive it to a `NeverQuasiHaltsSt tm_*` theorem, `make closeout`, and confirm the audit's "core undecided" drops from 59. **A board that does not move that number is not a board.** Only then do the other three, and only then the closer.

**GATE.** If the arm indexing cannot be stated so the interior arm and the fill arm share ONE scheme — one threshold, one stride, one offset, one lemma about `n = k + s*j` — stop and write §4l with the shape that resisted. Two parallel indexing schemes is the thing to avoid; it is how `fm_pre` became a fixed list in the first place. If `make closeout` will not take a board, fix `inventory.py`/`gen_stages.py` first; it is the difference between rows and files.

**DO NOT** add the Fibonacci-rank or terminator-template constructor, do not do the `(gray, 2)` `ClassSucc`, do not do the outer parameter (§4j), and do not touch the count language or `RU` — the two quadratic rows are not this session. Do not emit more boards before a machine-level theorem exists.

**CONSTRAINTS.** May touch `theories/Checkers/`, `theories/Machines/Ladder/`, `tools/ladder/`, `docs/`, `NEXT_SESSION.md`. **Never edit `theories/Census/`.** Do not touch tailcert/nestcert/regcert or wave-session files. Do not run a second session on `tools/ladder/valfam.py` while this one is live. Commit incrementally; push and open a PR when the first row moves the core count.

**Facts worth not rediscovering.**

* Coq is not in the image — `apt-get install -y coq` gives 8.18.0, which is what CI expects.
* **Never run `valfam.py` and `make -jN` at the same time.** A sweep that runs at 16 s/row against an idle box ran at 78 s/row against a build. The full tree is 2,665 files, ~2 h at `-j3`; `make closeout` is ~3.5 min once it is built. `.vo` are gitignored, so if your container already has them, a branch reset keeps them and a fresh container pays the 2 h.
* **§4k's lesson, and it is the cheap one:** before building anything that consumes the arms, run the arm builder over the rows and count. That single script showed the quasihalt port was worth 0 rows today and reversed the order of two sessions' work.
* The fill arm's guaranteed block copy must be materialised into `s_pre`. Measured: the canonical `rep u (1*j+1)` form finds **no chain at all**; `u ++ rep u j` finds one in six steps. Do not "clean up" that normalisation.
* `vis_of_run` wants a chain from the anchor, **not a prefix of the arm's own chain** — a state can sit inside a macro step no prefix ends on. `emit_closure` searches from every prefix of the fill's derivation for this reason.
* `emit_ladder.py` writes its refusal reason **into the `.v` file**. Read the file, not the driver's last line — `board_ladder.py` reports the last stdout line and that is often a bad-arm note, not the refusal.
* Keep `board_ladder.py`'s `wanted()` in step with `closure_data`'s refusals. It drifted once and silently refused 18 rows on a condition that had already been removed.
* The arm shapes in `core61_armshapes.txt` are from `lapcert.derive_chain` at `maxdepth 40 / nmax 400`. Raising those did not help any row that failed.
* `tools/ladder/bounce.py --selftest` exists because two versions of that probe silently missed both of the machines it was written to find. Any probe whose miss you intend to report needs a known-positive case.
* The arm-soundness and class lemmas are Closed under the global context — zero axioms — because they are on `csteps`/`cden` and never go through `lift`. Keep it that way; funext enters only in the final assembly.
