# v5-gap machines NOT boarded by ANY sound Coq checker: 1
# The 8 former scalar entries are now boarded by the v5c checker (block-hop
# closure + multi-run cell-stream end-match; the engine is UNCHANGED) --
# theories/Machines/ListCV5Stage/LCV5C_00..01.v, manifest
# tools/listc_v5c_manifest.tsv.
# The single remaining machine is the matrix-meta cert (v7, meta_a=0
# meta_b=0, a rulerunm lattice run): the scalar a*k+b meta layer that
# MetaBlkPfx and all forks reuse does not cover the matrix meta-map ("0 of
# the Phase-2 certs use the matrix meta-map").  Closing it needs the matrix
# meta layer ported -- a separate feature, orthogonal to the v5 gap.
# bin/verify passes it.  See docs/V5GAP_STAGE.md.
0RB0LA_1LC1LD_1RD0RD_1LA0RC
